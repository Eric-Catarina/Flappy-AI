using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FlappiesManager : MonoBehaviour
{
    [SerializeField]
    private Vector3 properStartingPosition = new Vector3(-7, 5, 40);

    [SerializeField]
    private GameObject flappyFab;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
